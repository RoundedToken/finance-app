# Post-MVP Roadmap

> Составлено 2026-05-29 после финализации MVP. Источник: многоосевой анализ (daily-usability / finance-depth / ai-coach / data-quality / tech-debt / infra-perf), заземлённый в реальном коде + `docs/review-mvp-stage1.md`.
>
> **Принцип:** сначала сделать продукт, которому **можно доверять каждый день** (фидбек, корректность данных, безопасность), потом добавлять **глубину**, и только по реальному триггеру — **продвинутое/масштаб**. Калибровка — single-user, без золочения.

## Цель

Полноценный daily-driver: каждое число на экране либо верно, либо честно помечено приблизительным; каждое действие даёт явный отклик; данные не теряются; аналитика отвечает на «сколько у меня / куда движется / сколько ещё можно».

---

## Фаза 1 — Доверие, фидбек, безопасность (дёшево, высокая отдача, фундамент)

> ✅ **Фаза 1 закрыта (2026-05-29).** 1.1 toast · 1.2 isError · 1.3 ErrorBoundary · 1.4 CI (зелёный) · 1.6 missing-rate/baseline бейдж · 1.7 restore-verify · 1.8 dedup rates-load · 1.9 trust-date — сделаны, задеплоены, визуально проверены. 1.5 (зарплата) отменён. Харнес дополнен сценариями ошибок (toast/list-error). Дальше — Фаза 2.

Кластер, на котором сошлись несколько осей. Почти всё — effort **S**, и закрывает риски, появившиеся/обнажившиеся после Стадии 2.

| # | Пункт | V/E | Почему первым |
|---|---|---|---|
| 1.1 | **Toast-фидбек на все мутации Web Admin** | H/S | 6 денежных страниц делают `mutateAsync` без `onError` → overdraft-400 (который Стадия 2 теперь **реально возвращает** с текстом «доступно X, нужно Y»), 5xx и обрывы сети молча проглатываются. Лекало уже в Mini App (ToastProvider + `e.message`). **Самый частый ежедневный риск молчаливой потери.** |
| 1.2 | **isError-состояния на списках Admin** | H/S | При 5xx таблицы показывают «данных пока нет» (маскируют ошибку под пустоту → риск перезавести дубли). Образец `ErrorState(onRetry)` уже в DashboardPage — распространить на 7 страниц. |
| 1.3 | **ErrorBoundary поверх Admin SPA** | M/S | Страховка от «белого экрана» при render-time throw. |
| 1.4 | **CI gate: typecheck + vitest на push/PR** (GitHub Actions, без автодеплоя) | H/S | `.github/workflows` нет вообще — 42 теста и 3 typecheck зелёные только вручную. Защищает ВСЮ дальнейшую разработку от тихих регрессий. Деплой остаётся ручным из merged main. |
| 1.5 | ~~Перевесить + дозаполнить зарплату~~ — **ОТМЕНЕНО** (Stepan: не важно) | — | Бэкфилл/перевешивание зарплаты не делаем. Честность истории закрывает trust-date бейдж (1.9) — «данные достоверны с YYYY-MM», без правки самих данных. |
| 1.6 | **missing_rates + buckets_without_baseline у KPI Net worth** | H/S | Главное число (net worth «сейчас») может молча недосчитывать позицию без курса; предупреждение спрятано в блоке «История». Поля уже в ответе — ~10 строк фронта. |
| 1.7 | **Restore-verification D1-бэкапа** | H/S | D1 — единственная живая копия. `backup_d1.py` сейчас проверяет лишь returncode → пустой/обрезанный дамп пройдёт молча. Импорт дампа в tmp sqlite + `COUNT(*)` порог. Закрывает риск необратимой потери. |
| 1.8 | **Убрать самоналожённые потери на чтении** | H/S | `loadRatesIndex` грузится дважды на `/accounts` (handler + внутри `listGoals`); `references` гоняет `listExpenses(20000)` + конверсию и выбрасывает. Бьёт по latency первого экрана + free-tier CPU **на каждом** входе в Admin. Чистый рефактор путей чтения. |
| 1.9 | **Trust-date бейдж «данные достоверны с YYYY-MM»** | M/S | Левая часть графиков (до baseline/курсов) — реконструкция, не факт. `markArea` + подпись. Снимает когнитивную ловушку «читаю приблизительное как точное». |

**Старт Фазы 1:** 1.1 (toast) → 1.2 (isError) → 1.4 (CI) — всё S, и закрывают доверие + защиту будущей работы. 1.5 (зарплата) — как только дашь RSD-суммы.

---

## Фаза 2 — Глубина и удобство (actionable-ценность)

> ✅ **В основном закрыта.** ✅ **2.1 Sparklines /accounts — закрыто (SPEC-021, 2026-05-31):** worker `net_worth_series.by_bucket_native` (нативный `balanceAt`, без новой математики/миграций); общие `components/Sparkline.tsx` + `lib/chart-theme.ts` (извлечены из дашборда); на карточке ведра — искра нативного баланса в цвете ведра, в сводке Net worth — искра `total_eur` (muted). Плоские/пустые вёдра без искры. Phase 3: qa=PASS_WITH_NICES, arch=APPROVED_WITH_NICES, 0 must-fix; визуально (light/dark + graceful при 5xx дашборда). ✅ **2.2 Бюджеты/лимиты — закрыто (SPEC-020, 2026-05-31):** таблица `budgets` (категория + общий потолок, EUR, recurring-месяц), `/v1/web/budgets` CRUD + bootstrap-подсказка, Admin `/budgets` (BudgetBar good/warn/over) + read-only бейдж остатка на плитке категории в Mini App. Phase 3: qa=PASS_WITH_NICES, arch=APPROVED_WITH_NICES, 0 must-fix; визуально проверено (Admin light/dark/modal, Mini App hint). Также закрыты: 2.5 (график прогресса цели, SPEC-037), 2.6 (D1-mock тесты, SPEC-022), 2.9 (адаптивные бюджеты, SPEC-023), 2.10 (порядок событий, SPEC-024), 2.11 (валюта↔счёт, SPEC-032) — см. таблицу. Не-AI часть Фазы 2 закрыта: 2.3 (мобильный ввод) — 🔻 в бэклог (owner: не нужно). Тех-долг: 2.8 ✅ SPEC-038, 2.7 auth-инвариант ✅ SPEC-039 (shared Zod-часть отложена — низкий ROI). AI: 2.4 ✅ SPEC-040 (шаг 1); шаг 2 (routine) — по триггеру.

| # | Пункт | V/E | Суть |
|---|---|---|---|
| 2.1 | ✅ **Sparklines баланса на карточках /accounts** (SPEC-021, хвост Stage 5b) | H/S | ~~Помесячный `balanceAt` уже считается — отдать ряд + мини-чарт под балансом ведра.~~ Сделано: `by_bucket_native` из dashboard-агрегатора → искра нативного баланса (цвет ведра) на карточке + искра net worth в сводке. |
| 2.2 | ✅ **Бюджеты/лимиты по категориям** (SPEC-020) | H/M | ~~Единственная отсутствующая поведенческая петля «сколько ещё можно потратить».~~ Сделано: `budgets` (категория + общий потолок), Admin `/budgets` + Mini App read-only подсказка. |
| 2.3 | 🔻 **Мобильный ввод дохода/снапшота через бота** — в бэклог (owner 2026-06-27: необходимости нет) | M/M | `bot.ts` умеет parse+write расход → можно было бы добавить `доход …` / `баланс …`. Но доход/снапшот вводятся периодически и с десктопа (Admin) — реальной дыры нет. Отложено без срока; вернуться, если появится боль. |
| 2.4 | ✅ **Coach: детектор анти-данных** (cron, БЕЗ LLM, SPEC-040, 2026-06-27) | H/S | ~~Раз в день: гэп без трат / устаревший snapshot / ведро без baseline → один `sendMessage`.~~ Сделано (AI-трек **шаг 1**): `coach.ts` (`signalsFor` чистое ядро) в `scheduled()` (cron `0 7`), правила качества данных + пороги бюджет/цель/free/runway + грубая аномалия; `coach_state` cooldown (миграция 0018); молчит когда ок. 13 vitest, qa=PASS/arch=APPROVED (1 must-fix 5xx закрыт). На проде. **Шаг 2** (умные инсайты/тонкие аномалии/ETA через Claude Code routine) — по триггеру «нуджи читаются». |
| 2.5 | ✅ **График прогресса цели на /goals/:id** (SPEC-037, 2026-06-27) | M/M | ~~Накопленный баланс цели по месяцам + линия target + дедлайн + ETA-пунктир.~~ Сделано: карточка «Прогресс» — клиентский cumulative по месяцам (в target_currency из `delta_in_target`) + markLine target/дедлайн + dashed ETA по run-rate завершённых месяцев (горизонт 10 лет, иначе «цель далеко»). ECharts, без worker/D1. Phase 3: arch=APPROVED, qa=PASS (1 must-fix закрыт). Визуально light/dark, R1 (последняя точка = balance). |
| 2.6 | ✅ **D1-mock тесты на getEffectiveBalance + dashboard** (SPEC-022) | H/M | ~~Деньги-critical wiring тестами не закрыт.~~ Сделано: мок `env.DB` на `node:sqlite` (real SQLite, грузит schema.sql) + 14 тестов (getEffectiveBalance: baseline/окно/5 событий/fee-JOIN; getDashboard: гард `by_bucket_native`, net worth, targeted/free, помесячный ряд). Mutation-sanity пройден. |
| 2.7 | 🟡 **withAdminSession-инвариант ✅ (SPEC-039) + shared Zod-контракт ⏸️** | M/M | ✅ **Часть 1 (SPEC-039, 2026-06-27):** 41 копия admin-guard'а → один префикс-guard `/v1/web/*` в роутере (нельзя забыть auth), хендлеры чистые, −77 строк, 12 auth-тестов через `worker.fetch`. ⏸️ **Часть 2 (shared Zod) отложена:** нет monorepo-workspace + worker-схемы это request-payload (response-типы admin'а не Zod) → `z.infer` покрыл бы ~10 типов ценой brittle cross-package — низкий ROI для single-user. Отдельный спек при необходимости. |
| 2.8 | ✅ **Валидация `limit` + cap на list/bulk** (SPEC-038, 2026-06-27) | M/S | ~~`?limit=abc` → `LIMIT NaN`; bulk-rates без cap.~~ Сделано: `parseLimit` (5 хендлеров), bulk-rates cap+skip, `limit_eur` cap, **+ бонус**: bootstrap rates-dedup (`loadRatesIndex` 3×→1×, скан всей таблицы rates на каждом входе Mini App). 169 vitest. arch=APPROVED, qa=PASS, 0 must-fix. Без миграций. |
| 2.9 | ✅ **Адаптивные бюджеты (RBAR)** — авто-рекомендация лимитов по истории (SPEC-023, Фазы 0+1+2) | H/XL | ~~Надстройка над 2.2: лимиты сами поджимаются/растут реалистично; накопительный конверт для редких покупок.~~ Сделано (2026-06-01): worker `rbar.ts`+`stats.ts` (движок, реплей из истории) + миграция 0012 + 4 эндпоинта advisory + Admin рекомендации/конверты/классификация-override + Mini App lens. 99 vitest (бэктест 29 мес + anti-drift). Phase 3: qa=PASS_WITH_NICES, arch=APPROVED_WITH_NICES (2 must-fix закрыты). ADR-016. Авто-применение (Фаза 3) + калибровка (Фаза 4) — отложены по дизайну. |
| 2.10 | ✅ **Детерминированный порядок событий во времени** (SPEC-024, 2026-06-01) | H/M | ~~Баг: операция, записанная после снапшота в тот же день, выпадала из баланса (граница `date > baseline.date`).~~ Сделано: tie-break по `created_at` при равной дате (дата — главный ключ, backdating сохранён) + канонизация формата `created_at` (миграция 0013, 1847 строк ISO→канон) + серверный `created_at` расходов + локальные даты операций/«сегодня» (`?today=`). Обе формулы баланса синхронны, 102 vitest. Phase 3: qa=PASS_WITH_NICES, arch=APPROVED_WITH_NICES (0 must-fix). ADR-017. Прод-импакт: 23 операции в день снапшота после него теперь учитываются. |
| 2.11 | ✅ **Согласованность валюта↔счёт при вводе траты** (SPEC-032, 2026-06-22) | H/S | ~~Баг: счёт и валюта в Mini App развязаны (валюта хардкод RSD, счёт липкий) → можно записать «RSD на RUB-ведре», баланс тихо искажается, сигнала нет.~~ Сделано: Mini App auto-bind (выбор счёта ставит его валюту) + осознанный override через confirm + amber-сигнал рассогласования + reconcile легаси-localStorage; Worker `createExpense`/`updateExpense` отклоняют немаркированное рассогласование (400). Аудит прод-данных: 1 запись (999 RSD на RUB-ведре) исправлена → 0 рассогласований. 157 vitest. Phase 3: arch=approved, qa=approved (0 must-fix). Визуально light/dark + EditScreen-легаси. |
| 2.12 | ✅ **Робастный месячный темп — медиана 6 мес** (SPEC-041 / ADR-022, 2026-07-01, ad-hoc по owner-фидбеку) | H/S | ~~Разовая трата ~4500 € (май) через среднее-за-3-мес на квартал ломала прогноз net worth (пунктир вниз при профицитном июне), норму сбережений (−15%) и runway.~~ Сделано: `windowSums` → медиана помесячных income/income_free/burn за 6 полных мес (переиспользован `median` из `stats.ts`), Δ-окна −6; попутно линза «Свободные» на графике net worth вычитает `invested_eur` по-точечно (синхронно с KPI-спарклайном, убран константный вычет-анахронизм); подписи «медиана за N полных мес». 199 vitest. Phase 3: qa=PASS_WITH_NICES, arch=APPROVED_WITH_NICES (must-fix закрыт до merge). Прод-импакт: норма free −15% → +24%, projectRate −546 → +877 €/мес. |

---

## Фаза 3 — Продвинутое / по триггеру (не делать впрок)

| # | Пункт | Триггер |
|---|---|---|
| 3.1 | **AI: anomaly-alert + free-form чат** «спроси про деньги» (tool-use поверх dashboard, LLM-клиент с prompt caching + жёсткий cost-cap, Claude Haiku / Workers AI) | После того как cron-nudge (2.4) докажет, что ты читаешь сообщения бота |
| 3.2 | **M3: распределение свободного потока между целями** (приоритет/доля → честный ETA) | Когда станет >1-2 активных целей одновременно |
| 3.3 | **Историч. реконструкция goal balance** (убрать аппроксимацию free-net-worth в прошлом) | После Фазы 1 (зарплата+trust-date); влияет только на Δ-бейдж и historic free-линзу |
| 3.4 | ✅ **Виртуализация (windowing)** Admin /expenses + Mini App История (SPEC-034, 2026-06-26) · 🔻 keyset-пагинация — по триггеру | Рендер-часть закрыта: `@tanstack/react-virtual`, в DOM только видимые строки (windowing-only, полный load + клиентские фильтры/KPI сохранены, Worker/D1 не тронуты). Keyset-пагинация отложена: при ~1.8k строк сеть не проблема, а фильтры/KPI клиентские → серверная пагинация без переноса фильтров на сервер бессмысленна. Триггер: рост данных 10× + готовность переносить фильтры на сервер |
| 3.5 | **Dashboard prefix-sum оптимизация** (O(buckets×events) вместо months×) | При 3-5+ годах данных / заметном лаге дашборда |
| 3.6 | **Прорежение/оконный SELECT для rates** | rates растёт ~5/день (cron) → десятки тысяч строк за годы |
| 3.7 | **Yield-счета (ожидаемый %) → Holdings акций/ETF + P&L** (Stage 9) | Когда реально появится депозит под % / портфель. Только отображение; авто-interest-tx НЕ делать (конфликт с manual=ground-truth) |

---

> **AI-трек — отдельным проектированием (Stepan, 2026-05-29).** НЕ платный Claude API. Цель — задействовать **routines Claude Code** (scheduled remote agents, входят в подписку): routine по cron читает D1 через worker-API и шлёт нуджи/инсайты в Telegram.
>
> ✅ **Шаг 1 сделан (SPEC-040, 2026-06-27):** детерминированные нуджи по качеству данных в Worker `scheduled()` (БЕЗ LLM) — проверяет гипотезу «нуджи читаются» дёшево, до вложений в routine. **Шаг 2** (умные инсайты/тонкие аномалии/ETA + free-form чат 3.1) — Claude Code routine (digest+notify эндпоинты), по триггеру после шага 1. До отдельного решения по routine LLM не реализуем.

## Сознательные non-goals (не делать)

- **AI:** weekly digest, goal check-in, onboarding-опрос + 3-мес re-check, `coach_profile`/`coach_surveys`, мульти-канальная доставка (Mini App push). Для одного юзера, который ежедневно открывает дашборд, — дублирование + золочение (review P4).
- **Worker → Pages Functions** миграция: топология работает, auth/CORS чисты — рефактор без пользовательской ценности и с риском задеть рабочую auth-связку.
- **Mini App расширение** за пределы ввода расходов — заморожено, **кроме read-only аналитики расходов** (экран «📊 Статистика», SPEC-036 / ADR-021, разморожено owner'ом 2026-06-27). CRUD снапшотов/доходов/обменов и не-расходная аналитика остаются вне Mini App (в Admin).
- **Расширение откатанных фич** (chains, goal-tagged tx, receipt scanner) без новой веской причины.

---

## Что нужно от Stepan'а

- ~~RSD-суммы зарплаты~~ — отменено (1.5 dropped, Stepan: не важно).
- **AI-трек** (2.4/3.1) — проектируем отдельно через routines Claude Code (подписка), не платный API.
- Приоритет не-AI части Фазы 2 (после Фазы 1): бюджеты (2.2) / sparklines (2.1) / мобильный ввод (2.3) / графики целей (2.5).

## Рекомендованный порядок

**Фаза 1 целиком** (закрывает доверие + безопасность + защиту разработки, почти всё S; без 1.5 и без AI) → затем не-AI часть Фазы 2 (sparklines 2.1, бюджеты 2.2) → AI-трек отдельно → дальше по сигналам Фазы 3. Тех-долг (2.6/2.7/2.8) вплетать между фичами под защитой CI.
