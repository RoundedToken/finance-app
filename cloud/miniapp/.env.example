# Скопировать в .env (gitignored) перед `npm run dev` / `npm run build`.

# URL твоего Worker. Persona-specific субдомен не хранится в публичном репо (docs/security.md).
VITE_API_BASE=https://<your-worker>.workers.dev
